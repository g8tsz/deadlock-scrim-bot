services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  bot:
    build: .
    depends_on:
      - mongo
    env_file:
      - .env
    restart: unless-stopped

volumes:
  mongo_data:
